#include "../../utils/utils.h"

void decimal_description(void) {
  text_blue("--------------------------------------------------------", 15);
  add_new_line(2);
  text_green("Decimal Conversion", 34);
  add_new_line(2);
  text_white("Convert decimal values to other system.", 24);
  add_new_line(2);
  text_blue("--------------------------------------------------------", 15);
}
